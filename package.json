{
  "name": "autotests",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test:setup": "npx playwright test cookies.setup.spec.ts && npx playwright test auth.setup.spec.ts",
    "test": "npm run test:setup && npx playwright test",
    "test:ui": "npx playwright test --ui"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.56.1",
    "@types/node": "^24.10.1"
  },
  "dependencies": {
    "@faker-js/faker": "^10.2.0"
  }
}
